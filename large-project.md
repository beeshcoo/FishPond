# FishPond · 大型项目开发（scale）

> "从零做一个大型项目 / 接手整个系统"时按本文件执行。目标不是"绝对 0 bug"（做不到），而是**零缺陷逃逸**：任何缺陷在合入前或上线前被硬门禁拦下，绝不静默溜进生产。**语言无关。**

## 诚实前提（先读）
```
✅ 能保证：零需求遗漏 · 零未验证交付 · 零静默失败 · 缺陷早拦（合入/上线前）
❌ 不能保证：字面意义的"永远不出 bug"——谁承诺谁在臆测
做法：把"必须真跑、必须有证据、必须可追溯"变成机器强制的门禁，而不是靠模型自觉。
```

## 全景流程（大项目 = 很多张卡片的有序编排）

```
需求/史诗(Epic) → 特性(Feature) → 模块卡片(Card) → 任务(Task)
        └─ 每一层都进追溯矩阵，每张卡片都过三道关，每次合入都过双层门禁 ─┘
```

## 第 0 步 · 计划模式（先聊清楚，只讨论不写代码）
和人对齐：目标用户 / 核心场景 / 边界（做什么·不做什么）/ 非功能要求。产出并**让人确认**五份真相源文档（模板见 [templates/](templates/)）：
`ARCHITECTURE.md`（总架构+服务清单）、`FEATURE_LIST.md`（功能清单）、`UI_UX.md`（UI/UX）、`DATA_MODEL.md`（表+字段）、`API_SPEC.md`（接口+服务）。**人签字确认后**才进第 1 步。

## 第 1 步 · 立项与骨架（一次）
1. **文档真相源就位**：第 0 步的五份文档进 `.fishpond/`。架构由人拍板，模型只填充与执行。
2. **画像与门禁**：产出 `.fishpond/PROJECT_PROFILE.md`（构建/测试命令**先真跑验证**）+ 装 `verify.sh` + git pre-commit（见 [enforcement/pre-commit](enforcement/pre-commit)）。
3. **CI 门禁**：用 [enforcement/ci.example.yml](enforcement/ci.example.yml) 把 `verify.（sh|ps1）` 接进 CI，并在分支保护里设为**必需检查**（PR 不绿不能合并）。**本地 pre-commit + CI = 双层拦截**，任何 AI/人都绕不过。

## 第 2 步 · 分层拆解（史诗 → 卡片路线图）
1. 把大目标拆成 **Epic → Feature → Card** 的有序清单（被依赖的排前），写进 `.fishpond/ROADMAP.md`。
2. 每条需求分配唯一 **需求ID**（如 REQ-001），登记进追溯矩阵 [templates/TRACEABILITY.md](templates/TRACEABILITY.md)。
3. **交人确认路线图**后才动手。大项目最大的风险是方向错，不是写得慢。

## 第 3 步 · 契约先行（让并行开发不打架）
- 在写实现前，**先定死所有跨模块接口契约**（API/事件/数据结构）并冻结进 `ARCHITECTURE.md`。
- 各模块**面向契约独立开发+独立测试**（可 Mock 依赖）；契约不变，谁都不阻塞谁。
- 改契约 = 大事：必须评审 + 更新架构 + 通知被依赖方 + 兼容处理（不删字段/加版本）。

## 第 4 步 · 逐卡交付（一次一张，跑绿再下一张）
对每张卡片走 [dev.md](dev.md) 的 Phase 1→5，且必须满足下面的**完成定义(DoD)**才算完：

```
Definition of Done（每张卡片硬门禁，缺一不算完）
☐ 1. 卡片全部验收标准都有对应自动化测试，且真实跑绿（贴输出+退出码）
☐ 2. 正常 + 异常 + 边界 三类路径都有测试
☐ 3. 过 review.md 三道关（含安全项），无阻断项
☐ 4. 契约与 ARCHITECTURE.md 一致；有变已同步更新
☐ 5. 全量回归绿（不只跑本模块），无回归
☐ 6. 追溯矩阵已更新：需求ID ↔ 卡片 ↔ 测试 ↔ 证据
☐ 7. pre-commit + CI 双门禁通过
☐ 8. 交付物齐全：API/DB 变更清单 + CHANGELOG + DEVLOG 追加
☐ 9. 文档 0 误差对齐：FEATURE_LIST ↔ DATA_MODEL ↔ API_SPEC ↔ UI_UX ↔ NAVIGATION ↔ 实际代码 一致
☐ 10. 公开接口/复杂逻辑有充分注释，命名用领域语言；关键代码有 [F-xxx]/错误码锚点
☐ 11. 测试覆盖率达到 PROJECT_PROFILE 设定阈值（verify 脚本内硬判定，未达标即失败）
```

## 一致性对齐门禁（0 误差交付的核心）
大项目最隐蔽的缺陷是"文档和代码对不上"，导致接手的人被误导。每次交付逐条核对：
```
☐ FEATURE_LIST 每条功能 → 代码有实现、有测试、有对应接口与表
☐ DATA_MODEL 每表每字段 → 与迁移脚本、ORM 实体逐字段一致
☐ API_SPEC 每个接口 → 路径/方法/入参/出参/错误码/鉴权与代码一字不差
☐ 无"影子功能/影子接口/影子字段"（代码有、文档无）或反向（文档有、代码无）
不一致的每一处都记为缺陷，修到一致才算交付。
```

## 第 5 步 · 集成与回归
- 按依赖拓扑合入；每合入一张跑一次**全量回归**。
- 回归红 → 立即停、定位、修，**不许带红前进**（大项目里带红前进 = 雪崩）。

## 第 6 步 · 发布可靠性（上生产前）
```
☐ 灰度/分批发布（能小范围先放量）
☐ 一键回滚方案（写清命令 + 触发条件）
☐ 可观测：关键指标 + 错误告警 + 日志（出问题能立刻发现）
☐ 数据迁移可回退 / 有备份
☐ 冒烟脚本：上线后自动验核心链路
```

## 接手存量大项目（棕地）
```
1. 别一次重写。先用 ARCHITECTURE.md 增量测绘：一次搞清一个模块，逐步补全模块地图。
2. 先给"要动的那块"补上表征测试(characterization test)，锁住现有行为，再改。
3. 用 strangler(绞杀者)模式：新功能走新路径，旧路径逐步替换，每步可回退。
4. 每动一块就更新架构 + 追溯矩阵 + LESSONS。
```

## 第 7 步 · 交接就绪（AI 做完，开发人员直接上手）
交付即"能被别人接手"。**统一入口是 `HANDOFF.md`（模板 [templates/HANDOFF.md](templates/HANDOFF.md)），边开发边写、逐步填实，不到交付才拼凑。** 确保：
```
☐ HANDOFF.md 全填实：环境搭建每条命令亲测有效；每个"见 XXX"都指向真实有内容的文件
☐ NAVIGATION.md 定位地图可用：随机抽功能/报错码/字段，能 30 秒定位到代码/表/接口
☐ .fishpond/ 文档齐全且与代码 0 误差对齐（架构/功能清单/数据模型/接口/UI-UX）
☐ DEVLOG 能让人看懂"做到哪、为什么这么做、下一步"
☐ 代码有充分注释；关键决策在 ARCHITECTURE 的决策表里可查
☐ 已知问题/待办诚实列出，不藏
☐ 找个没参与的人照 HANDOFF 跑一遍，文末"交接确认清单"全绿才算交接成功
```

## 大项目里治 DeepSeek 的三条硬规矩
```
1. 一次只喂一张卡片的上下文（别把整个系统塞给它，它会乱猜、会越界）。
2. 结论必须带真实命令输出；没输出=没完成（CI 会二次证伪）。
3. 拿不准就停下来问人；契约/架构/表结构一律由人拍板。
```
