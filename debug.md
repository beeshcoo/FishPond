# FishPond · 生产环境排查（debug）

> 用户说"线上出问题 / 帮我排查 / 看看服务器"时按本文件执行。**任何技术栈通用**。

## 红线（先默念）
```
🔴 生产 = 用户真金白银
📋 先诊断 → 出报告 → 等确认 → 再动手
🔧 止血（立即恢复）+ 治本（永不再犯），两者都要
🔄 修完同步回本地/仓库（本地 = 标准版）
🔒 凭据用后即弃：仅本次会话使用，结束后不再保留；下次需要重新索要，绝不写进代码/日志/提交
```

## 第一步 · 索要连接信息（一次性）
```
我需要以下信息来连接生产环境：
1. 接入方式：SSH / 云控制台 / kubectl / 远程桌面 …
2. 服务：部署方式（systemd/容器/K8s/云函数）/ 日志查看方式 / 配置路径
3. 数据库（若需查数据）：连接方式 + 库名（+ 临时凭据）
4. 问题：什么现象？何时开始？最近有无变更/发布？

⚠ 这些信息仅用于本次排查，会话结束即不再保留；下次需要会重新向你索要。
```

## 第二步 · 诊断
> **先用定位地图 `NAVIGATION.md` 把现象落到具体代码/表/字段/接口**（有报错码查错误码表 → 直达抛出位置；数据不对查字段反查表 → 找到写入它的功能/接口）。这一步能省掉大量瞎找。
```
1. 定范围(~3 分钟)：服务状态 → 端口/健康检查 → 近30分钟日志找第一个 ERROR 时间 → CPU/内存/磁盘/连接数。
2. 定根因(10–20 分钟)：用 NAVIGATION 定位现场 → 从堆栈找最底层异常(不是最外层)；还原调用链；看近期变更(版本控制历史)；查数据异常(NULL/非法状态/量级)。
3. 出《诊断报告》：
   ### 现象：用户看到什么
   ### 直接原因：最底层异常的代码位置 + 行号
   ### 根因(5 Whys)：为什么×5 → 系统性根因
   ### 影响数据：多少条记录/多少用户受影响
   ### 修复方案：见第三步
```

## 第三步 · 修复方案（止血 + 治本）
```
### 止血（立即，尽量不需部署）
[具体命令，每行带注释] + 预计耗时 / 停机时间 / 回退方案

### 治本（改代码，需部署）
[具体 diff：文件路径 + 行号] + [测试命令]

### 全局影响分析（逐项"是/否+说明"）
| 会影响其他模块吗 | API 响应格式会变吗 | 已有数据受影响吗 | 前端展示受影响吗 | 性能受影响吗 |

### 执行步骤
1.[ ]备份数据 2.[ ]止血 3.[ ]验证 4.[ ]治本(如需部署) 5.[ ]同步本地 6.[ ]复盘
```

## 第四步 · 请求确认（未确认不得改生产）
```
输出："止血[简述]→X 分钟→风险[低/中]；治本[简述]→需部署→风险[低/中]；全局影响[无/有]。是否继续？"
用户确认 → 执行；用户说"直接更新生产" → 止血→治本→部署→验证；用户拒绝 → 给替代方案。
```

## 第五步 · 验证 + 同步
```
1. 验证：健康检查通过 / 原问题不复现 / 错误日志干净 / 核心功能冒烟。
2. 同步本地：把生产修复回流本地仓库，commit + push（本地=标准版）。
3. 收尾汇报：✅已恢复 / 📋根因一句话 / 🔧修复 commit hash / 📝规范补充 / 🔄本地已同步。
4. 记录：把这次故障追加到 .fishpond/LESSONS.md，防同类复发。
```

## 不同部署环境的排查入口（按实际选用）
```
systemd  ：systemctl status / journalctl -u <svc> --since "30 min ago"
容器      ：docker ps / docker logs --since 30m <container>
K8s       ：kubectl get pods / kubectl logs --since=30m <pod> / kubectl describe pod
云平台     ：对应云监控与日志服务（函数日志 / APM / 指标面板）
数据库     ：只读连接优先；改数据前必须先备份
```

## 治本原则
```
止血 ≠ 治本。治本必须：修根因(非症状) + 全局影响分析(查所有引用点) + 测试验证 + 回退方案 + 教训补进工程规范。
```

## 禁止
```
❌ 未确认直接改生产   ❌ 在生产上做实验     ❌ 跳过备份直接改数据
❌ 只看表象不挖根因   ❌ 只看当前文件不查全局引用   ❌ 保留/存储凭据   ❌ 跳过同步本地
```
