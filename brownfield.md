# FishPond · 接手旧项目（棕地 / Brownfield）

> "接手已有项目 / 旧系统改造 / 拆成很细的块"时按本文件执行。
> 目标：**不改崩全局**——先测绘地图、再锁现状、后小步替换。
> **语言无关。** 详细话术见 [USAGE.md §6.5](USAGE.md)。

## 诚实前提
旧项目最大的风险不是"写得慢"，而是**改一处坏全局、接手找不到表/接口/代码**。本模式用四步解决。

## 四步流程（必须按顺序）

### 第 1 步 · 圈鱼塘（只测绘，禁止改业务代码）
产出并让人确认：
- `.fishpond/PROJECT_PROFILE.md`（构建/测试命令**先真跑**）
- `.fishpond/ARCHITECTURE.md`（现有模块地图/依赖/技术债）
- `.fishpond/NAVIGATION.md`（现有功能/接口/表/字段/错误码/代码位置全登记）

**本步禁止**：修改任何业务逻辑代码（允许只加 `.fishpond/` 文档与门禁配置）。

### 第 2 步 · 圈小范围，拆成非常细的卡片
- **不要一次拆整个系统**；只圈"本轮要动"的范围（如"组织中心""审批模块"）。
- 产出 `.fishpond/ROADMAP.md` + `.fishpond/cards/*.md`（模板 [templates/CARD.md](templates/CARD.md)）。
- 每张卡：只做一件事、能独立测试、标注现有代码位置与风险、是否动公共接口。
- **交人确认清单**后才动手。

### 第 3 步 · 表征测试（characterization test）锁现状
动第一张卡之前：
- 给涉及的**现有功能**补测试，锁住当前真实行为并跑绿。
- 改动前后都跑这些测试 + 全量回归——碰坏别处立刻红。

### 第 4 步 · 绞杀者模式（strangler）逐卡替换
- 一次只改一张卡范围内的文件；不动其他模块/公共接口（要动先授权）。
- 新逻辑走新路径，旧路径逐步替换，**每步可回退**。
- 每卡：真跑测试贴输出 → 更新 NAVIGATION/DEVLOG/HANDOFF → 过 pre-commit → commit。
- **一张没跑绿，不许开下一张。**

## 可直接复制给 AI 的四段话术
见 [USAGE.md §6.5](USAGE.md)——四段原话可直接粘贴。

## 旧项目 DoD（每张卡额外要求）
```
☐ 全局影响分析已做（引用点/被依赖方已查）
☐ 表征测试存在且改前改后都绿
☐ 全量回归绿（不只本模块）
☐ NAVIGATION 已更新（新/改的接口/表/错误码/代码位置）
☐ 无"影子功能"（代码有、文档无）
```

## 与 large-project 的关系
- 棕地 = large-project 的特殊入口：跳过"从零计划模式"，从**测绘 + 小范围拆块**开始。
- 测绘完成后，单张卡的开发仍走 [dev.md](dev.md) Phase 1→5。
