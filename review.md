# FishPond · 代码审查（review）

> "审查这个模块 / review this"时按本文件执行。先跑通用清单；命中对应技术栈再套附录。**语言无关**。

## 第一步 · 确定范围
```
指定模块名 → 审查该模块全部文件
指定文件   → 审查该文件
都没指定   → 审查当前 diff（git diff <主干>...HEAD）
```

## 第二步 · 通用清单（语言无关，21 项）

### A. 模块边界（3）
```
☐ 1. 模块目录/包边界清晰，公开面仅通过声明的接口暴露
☐ 2. 跨模块依赖"显式声明"，无隐式引用（对照卡片"依赖模块"）
☐ 3. 无循环依赖（有架构测试就跑；没有就人工画依赖图确认）
```
### B. API 契约（4）
```
☐ 4. 路径/方法与卡片一致（名词式 RESTful）
☐ 5. 每个端点都有鉴权（公开端点必须显式标注为公开）
☐ 6. 请求入参有校验（必填/长度/格式），错误消息人能看懂
☐ 7. 响应用专门 DTO/schema，不裸吐数据库实体；对外向后兼容（不删字段/不改签名）
```
### C. 数据层（4）
```
☐ 8. 迁移版本号连续、命名规范；破坏性改表(改类型/改名/删列)用 expand-contract 分步，不一把梭
☐ 9. 新表有审计列(created_at/updated_at)；需并发控制的有乐观锁版本列
☐ 10. 外键显式声明并命名(fk_<表>_<列>)
☐ 11. 外键列与常用 WHERE/ORDER BY 列有索引
```
### D. 异常与质量（4）
```
☐ 12. 自定义异常带错误码；有统一异常处理器覆盖它们
☐ 13. 无 N+1 查询（关联用 JOIN/预加载；仓库不在循环里调）
☐ 14. 写操作有事务；纯读走只读
☐ 15. 无臆测/占位/TODO 假装完成；无被注释掉的死代码
```
### E. 安全（3）
```
☐ 16. 无硬编码密钥/密码/token（grep 常见关键字：password/secret/token/apikey）
☐ 17. 无 SQL 注入风险（用参数化查询/ORM，不字符串拼 SQL）；无命令注入
☐ 18. 用户输入在输出到前端/日志前做了转义/脱敏；越权已在服务端校验（不是只藏前端按钮）
```
### F. 文档一致性 · 可交接 · 可定位（3）
```
☐ 19. 文档↔代码 0 误差：本次改动涉及的 FEATURE_LIST/DATA_MODEL/API_SPEC/UI_UX/NAVIGATION 已同步，无影子功能/接口/字段
☐ 20. 可交接：公开接口/复杂逻辑有注释（讲"为什么"）；命名领域化；DEVLOG 已追加
☐ 21. 可定位：功能入口有 [F-xxx] 锚点、错误码可 grep、NAVIGATION 的代码位置真实存在——随机抽 1 行能 30 秒定位到现场
```

## 第三步 · 输出报告
```markdown
# 审查报告：<模块名>
## 必须修复（阻断合入）
- [ ] 第X项：具体问题 + 文件:行号 + 修复建议
## 建议改进（不阻断）
- [ ] 第Y项：优化建议
## 合规通过
- ✅ 第 1,2,3… 项
```

## 第四步 · 判定
```
全部通过     → ✅ 通过，进入集成
1–2 项未过   → ⚠️ 修正后复审
3+ 项未过    → ❌ 驳回，回头检查模块卡片本身是否有问题
```

## 快速审查（mini，只查 6 项）
边界(1)、依赖显式(2)、端点鉴权(5)、迁移连续(8)、事务(14)、无硬编码密钥(16)。

---

## 附录 · 各语言具体检查（命中哪个套哪个，地位平等）

### Java / Spring
```
☐ 模块声明与 allowedDependencies 显式；架构测试(如 ArchUnit)通过
☐ Controller 参数 @Valid；DTO 字段 @NotNull/@NotBlank/@Size，返回 record 不返回 @Entity
☐ Service 写方法 @Transactional，读 @Transactional(readOnly=true)
☐ @ManyToOne/@OneToMany 有 JOIN FETCH 或 @EntityGraph；端点有 @PreAuthorize
```
### Go
```
☐ 包边界清晰，internal/ 用于私有；无跨 internal 引用
☐ error 显式处理不吞（不 _ = err）；错误用 %w 包装可 errors.Is/As
☐ 导出符号有 doc 注释；context.Context 作首参传递并遵守取消
☐ 并发有 -race 通过；DB 用 sqlx/参数化，不拼 SQL；handler 校验入参
```
### Python
```
☐ 有类型注解且 mypy/pyright 通过；无裸 except；异常带上下文
☐ Pydantic/schema 校验入参；ORM 用参数化查询
☐ 依赖注入清晰，无模块级副作用；async 无阻塞调用混用
☐ 端点有鉴权装饰器/依赖；返回 schema 不返回 ORM 对象
```
### TypeScript / 前端
```
☐ 无 any 滥用；类型来自共享定义，前后端契约一致
☐ 组件状态：normal/loading/empty/error/forbidden 都覆盖；无未处理 Promise
☐ 无 dangerouslySetInnerHTML 未净化；无密钥打进前端包
☐ 复用设计系统组件与 token，不重复造轮子；可访问性(aria/键盘)基本满足
```
> 其他技术栈按同样格式在此追加，各语言地位平等，主流程不偏向任何语言。
