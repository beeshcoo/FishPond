# FishPond 使用指南（非常详细）

本文件教你**一步一步**怎么用 FishPond。读完你能：装好 → 触发 → 从零做完一个项目 → 交接。
适用于 **Claude Code（可接 DeepSeek 或任意模型）/ Cursor** 等支持 Agent Skills 的工具。

---

## 0. 一分钟理解它是什么

FishPond 不是帮你写代码的"魔法"，而是一套**纪律 + 文档 + 门禁**，把易出错的大模型"圈"在一小块可控范围里：
- **人**负责拍板（架构、边界、要做什么）。
- **模型**只在你确认过的"模块卡片"范围内写代码 + 自检。
- **门禁**（git pre-commit + CI）负责：测试不绿 / 覆盖率不达标 → **提交不了、合并不了**。

所以用它的核心动作只有三个：**① 说触发词 → ② 在计划模式里和它敲定文档 → ③ 一张卡片一张卡片地跑，让门禁把关。**

---

## 1. 确认已安装

- 已安装位置：`~/.claude/skills/fishpond/`（Claude Code）或 `~/.cursor/skills/fishpond/`（Cursor）。
- 没装就装：
  ```bash
  git clone https://github.com/beeshcoo/FishPond.git
  cd FishPond
  ./install.ps1          # Windows；mac/linux 用 ./install.sh
  ```
- **装完重启一次 Claude Code / VS Code**，让它扫描到新技能。
- 验证：新开一个对话，输入 `用 FishPond 帮我看下今天怎么开始`，如果它开始按 FishPond 的方式回你，就说明技能已加载。

> 原理：工具读取 `SKILL.md` 的 `description`，命中触发词就自动把技能内容加载进上下文。你**不需要**手动 @ 或贴文件。

---

## 2. 触发词一览（说这些话就会启动）

| 你想做什么 | 直接说（示例） | 会走哪套流程 |
|---|---|---|
| 从零做整个系统 | "用 FishPond 做一个 XX 系统" | 计划模式 + [large-project.md] |
| 加/改一个模块 | "用 FishPond 开发 XX 模块" | [dev.md] |
| 审查代码 | "用 FishPond 审查 XX 模块 / review 这段" | [review.md] |
| 线上排查 | "线上 XX 出问题了，用 FishPond 帮我排查" | [debug.md] |
| 看范例 | "给我看 FishPond 的模块卡片例子" | [examples.md] |

> 只要句子里带 "FishPond / 圈鱼塘 / 模块卡片"，或明显是"开发某模块 / 审查 / 线上排查"，技能就会介入。

---

## 3. 实战演练 A：从零做一个大型项目（最完整）

以"做一个报销审批系统"为例，走一遍你和 AI 各干什么。

### 步骤 1 —— 启动计划模式
你说：
```
用 FishPond 帮我做一个"报销审批系统"，我先跟你聊需求。
```
AI 会**只讨论、不写代码**，问你：目标用户、核心场景、边界（做什么/不做什么）、非功能要求（性能/安全/合规）。
👉 你的任务：**如实回答，把边界说清楚**。含糊的地方它会追问——别嫌烦，这一步省掉后面 80% 返工。

### 步骤 2 —— 确认五份"真相源"文档
聊清后，AI 会在项目根生成 `.fishpond/` 并产出让你确认：
- `ARCHITECTURE.md` 总架构（模块划分/依赖/技术选型）
- `FEATURE_LIST.md` 功能清单（每条功能一个 ID，如 F-001 提交报销单）
- `UI_UX.md` 界面规格（页面/交互/7 种状态）
- `DATA_MODEL.md` 数据库表+字段
- `API_SPEC.md` 接口+服务清单

👉 你的任务：**逐份看、拍板**。不满意就让它改。**你签字确认后**才允许写代码。这是"人做决策"。

### 步骤 3 —— 装门禁（一次）
在项目目录执行：
```powershell
# 到 FishPond 克隆目录里跑，指向你的项目
./install.ps1 -Project C:\path\to\报销系统
```
然后打开项目里的 `.fishpond/verify.ps1`（或 `verify.sh`），把占位命令换成真实的：
```
构建命令：例如  pnpm build      （前端）/  mvn -q compile （Java）
测试命令：例如  pnpm test --coverage  /  mvn -q test
```
从此**测试不绿就 commit 不了**。再把 `enforcement/ci.example.yml` 复制到项目 `.github/workflows/ci.yml`，在 GitHub 分支保护里设为必需检查 → **CI 也拦一道**。

### 步骤 4 —— 让它拆卡片（分块）
你说：
```
把功能清单拆成有序的模块卡片清单给我确认。
```
AI 产出 `ROADMAP.md`：Epic→Feature→Card，被依赖的排前面。
👉 你的任务：**确认顺序**。确认后开工。

### 步骤 5 —— 一次一张卡，逐张跑
你说：
```
开始做第一张卡：user-auth（登录鉴权）。
```
AI 会：写迁移→实体→仓库→DTO→服务→接口，写测试，**真实运行测试并把输出贴给你**，同步更新 `DATA_MODEL/API_SPEC/FEATURE_LIST/NAVIGATION/DEVLOG`，在代码入口留 `// [F-001]` 锚点，然后 commit（过门禁）。
👉 你的任务：**看它贴的测试输出**（有没有真跑、是否全绿）。**一张没跑绿，不许它开下一张。**

### 步骤 6 —— 重复直到做完，然后交接
最后你说：
```
生成交接清单，确保新人能直接上手。
```
AI 产出 `HANDOFF.md`：一步一命令的环境搭建（都亲测过）、结构导览、定位速查、已知问题。
👉 验收标准：**找个没参与的人，只看 `.fishpond/HANDOFF.md` 就能把项目跑起来**。

---

## 4. 实战演练 B：给已有项目加一个模块

```
用 FishPond 在这个项目里新增"优惠券"模块。
```
AI 会：先读 `.fishpond/`（恢复记忆）→ 写一张 coupon 模块卡片给你确认 → 按文件顺序开发 → 真跑测试 → 更新文档 → 过门禁提交。
👉 你只需：确认卡片、看测试输出。

---

## 5. 实战演练 C：代码审查

```
用 FishPond 审查 coupon 模块。
```
AI 按 21 项通用清单 + 对应语言附录逐条检查，输出报告：**必须修复 / 建议改进 / 合规通过**，每条带文件:行号。
👉 你只需：让它把"必须修复"改掉再复审。

---

## 6. 实战演练 D：生产排查

```
线上报销提交接口一直 500，用 FishPond 帮我排查。
```
AI 会先向你要连接信息（用完即弃），先用 `NAVIGATION.md` 把现象落到具体代码/表/字段，出《诊断报告》（现象/直接原因/根因 5 Whys/影响数据/止血+治本方案），**等你确认后**才动生产。
👉 你只需：提供访问方式、确认修复方案。

---

## 6.5 实战演练 E：接手旧项目，拆成很细的块（棕地，重点）

这是最容易翻车的场景（改一处坏全局）。FishPond 的做法是**先测绘、再锁定、后拆块，绝不一上来大改**。

### 第 1 步 · 先"圈鱼塘"——只读、只测绘、不改代码
复制这段发给它：
```
用 FishPond 接手这个已有项目。先只做"圈鱼塘"：
1) 扫描技术栈，生成 .fishpond/PROJECT_PROFILE.md（构建/测试命令先真跑验证）；
2) 通读代码，增量生成 .fishpond/ARCHITECTURE.md 的模块地图（现有模块/边界/依赖）；
3) 生成 .fishpond/NAVIGATION.md：把现有的功能、接口、表、关键字段都登记进去。
本步禁止修改任何业务代码，只产出文档，完成后给我看。
```
👉 产出一张"这项目到底有什么"的地图。做完这步，"表/库/字段/接口找不到"就没了。

### 第 2 步 · 指定小范围，拆成很细的卡片
**不要让它一次拆整个系统**，只圈你这轮要动的那块：
```
我这轮只改"XX 这一块"。用 FishPond 把它拆成非常细的模块卡片清单，写进 .fishpond/ROADMAP.md：
- 每张卡只做一件事、能独立测试；
- 按依赖拓扑排序（被依赖的在前）；
- 每张卡标注：涉及的现有表/接口/代码位置（引用 NAVIGATION）、风险、是否改动公共接口。
先只给清单，别写代码，等我确认。
```
👉 产出一份"细到每张卡都能单独跑"的拆块清单，你审顺序和范围。

### 第 3 步 · 改之前先补"表征测试"锁住现状
```
动第一张卡之前，先给它涉及的现有功能补表征测试（characterization test），
把现在的真实行为锁住并跑绿，再开始改。改动前后都跑这些测试。
```
👉 **先锁住现状**：改这块时一旦碰坏别处，测试立刻红——这就是"改一块也顾全局"的机器保障。

### 第 4 步 · 一次一张卡，绞杀者模式渐进替换
```
开始做第一张卡。要求：一次只改这张卡范围内的文件；不改其他模块和公共接口（要改先告诉我）；
新逻辑走新路径、旧路径逐步替换、每步可回退；改完真跑测试贴输出，并更新 NAVIGATION/DEVLOG。
```
👉 你把关：有没有越界、有没有真跑测试。一张跑绿再下一张。

### 一句话
**测绘地图(1) → 圈小范围拆细卡(2) → 补表征测试锁现状(3) → 一张一张绞杀式替换(4)。** 旧项目也能做到"改一块不坏全局、且每块都找得到"。

---

## 7. 门禁到底怎么工作（重要）

```
你 git commit
   └─> pre-commit 钩子自动触发
        ├─ 扫描暂存区是否有密钥 → 有则拦
        └─ 跑 .fishpond/verify.(ps1|sh)：构建 + 测试 + 覆盖率
             ├─ 全绿 → 允许提交 ✅
             └─ 失败 → 阻断提交 ❌（改到绿再来）
你 push / 开 PR
   └─> CI 再跑一遍 verify → 不绿不能合并
```
- 紧急绕过（清楚后果时）：`FISHPOND_SKIP=1 git commit ...`
- 这套对 **DeepSeek、Claude、人类一视同仁**——它没法"假装测试过了"。

---

## 8. 用 DeepSeek 时的三条黄金提示

1. **一次只喂一张卡**：别说"把整个系统写了"，说"做第 N 张卡"。上下文越窄，它越不容易乱猜、越不越界。
2. **每次都要它贴真实输出**：看到"测试通过"就追问"贴出你刚运行的命令和输出"。没输出=没完成。
3. **拿不准让它停下来问你**：卡片里没写清的（表结构/技术选型/契约），它必须问，不许自己拍脑袋。

---

## 9. 常见问题 FAQ

- **Q：我不想每次都走全套，改个文案也要卡片吗？**
  A：不用。FishPond 有任务分级——🟢微改动直接改+跑一次相关测试即可，只有🟡单模块/🔴跨模块才走全流程。

- **Q：`.fishpond/` 要提交到 git 吗？**
  A：要。它是项目的文档真相源和跨会话记忆，团队/下次会话都靠它。（`verify` 里别写死密钥即可。）

- **Q：换了新会话，AI 忘了项目怎么办？**
  A：它开工会先读 `.fishpond/`（架构/功能清单/DEVLOG/LESSONS）恢复记忆。你也可以直接说"先读 .fishpond 再继续"。

- **Q：不是 Java 项目能用吗？**
  A：能。主流程与模板语言无关；`review.md` 有 Go/Python/前端等平等附录，`verify` 填你自己项目的命令即可。

- **Q：怎么确认它没糊弄我？**
  A：三看——看它有没有贴真实测试输出、看 `HANDOFF.md` 的命令能不能真跑、看文档和代码是否一致（NAVIGATION 抽一行能否 30 秒定位）。

---

## 10. 速查

```
装到工具：      ./install.ps1            (加 -Target both 同时装 Cursor)
给项目装门禁：  ./install.ps1 -Project <项目路径>
开始大项目：    “用 FishPond 做一个 XX 系统”
加模块：        “用 FishPond 开发 XX 模块”
接手旧项目：    “用 FishPond 接手这个已有项目，先只圈鱼塘测绘、不改代码” → 再“把 XX 这块拆成很细的卡片”
审查：          “用 FishPond 审查 XX 模块”
排查：          “线上 XX 出问题，用 FishPond 帮我排查”
紧急跳过门禁：  FISHPOND_SKIP=1 git commit ...
```
