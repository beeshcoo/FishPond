# 交接清单 (Handoff)

> 放在 `<项目>/.fishpond/HANDOFF.md`。**边开发边写、逐步填实**，不是交付前糊弄拼凑。
> 验收标准：一个**没参与过**的开发人员，只看本文件 + `.fishpond/` 文档，能在约定时间内把项目**跑起来、看懂结构、继续开发**。
> 铁律：每条命令都**亲自真跑验证过**；每个"见 XXX"都指向真实存在且有内容的文件；不写"应该""大概""参考网上"。

---

## 0. 一句话与现状
- 这是什么：<系统定位，服务谁，解决什么>
- 当前进度：<已完成哪些功能 / 进行中 / 未开始>（详见 `FEATURE_LIST.md`）
- 能不能上线：<能 / 不能 —— 原因>

## 1. 技术栈与版本（照抄可用）
| 项 | 值 | 备注 |
|---|---|---|
| 语言/运行时 | <如 Node 20.x / JDK 21> | 版本不对会跑不起来 |
| 包管理 | <pnpm 9 / Maven> | |
| 数据库 | <PostgreSQL 16> | |
| 其他 | <Redis / MQ / …> | |

## 2. 环境搭建（一步一命令，均已真跑验证）
```bash
# 1) 克隆
git clone <repo-url> && cd <project>
# 2) 安装依赖
<真实命令>            # 例：pnpm install
# 3) 准备配置（见第 3 节）
cp .env.example .env  # 然后填写第 3 节表格里的值
# 4) 起数据库/中间件
<真实命令>            # 例：docker compose up -d db
# 5) 跑数据库迁移
<真实命令>
# 6) 启动后端
<真实命令>            # 监听端口：<8080>
# 7) 启动前端
<真实命令>            # 访问：http://localhost:<port>
```
> 常见启动坑：<列出你踩过的，如端口占用/Node 版本/环境变量缺失> —— 详见 `LESSONS.md`。

## 3. 配置与密钥（哪来、填哪、含义）
| 变量 | 含义 | 从哪获取 | 是否必填 | 示例(脱敏) |
|---|---|---|---|---|
| DATABASE_URL | 数据库连接 | 本地固定 / 运维提供 | 是 | postgres://user:***@localhost:5432/app |
| JWT_SECRET | 令牌签名密钥 | 自行生成 | 是 | *** |
> ⚠ 真实密钥绝不写进本文件/仓库；这里只写"从哪拿"。

## 4. 项目结构导览（找东西的地图）
```
<目录树，标注每个关键目录/模块放什么>
src/user/        用户与鉴权（对外 /api/v1/auth）
src/order/       订单
...
.fishpond/       项目文档真相源（架构/功能/数据/接口/日志）
```
- 想改某功能，先看：`FEATURE_LIST.md` 找到功能ID → 对应模块与卡片 → 代码位置。
- 想懂全局：先读 `ARCHITECTURE.md`。

### 4.1 定位速查（出问题/找东西先看这里 → `NAVIGATION.md`）
```
找某功能的代码   → NAVIGATION 主索引查功能ID → grep 该 [F-xxx] 锚点
排查某报错       → NAVIGATION 错误码表 → 直达抛出位置 + 触发条件
查某字段谁在改    → NAVIGATION 表/字段反查 → 定位实体与所有读写点
找某接口实现     → grep 路径关键字 或 handler 领域名（如 createOrder）
查某表结构       → DATA_MODEL.md（字段/约束/注释）
数据出问题       → 字段反查写入它的功能/接口 → 复现 → 修（配合 debug.md 根因流程）
```

## 5. 如何运行与验证（冒烟）
```
- 健康检查：<GET /health 期望返回>
- 核心链路冒烟：<登录 → 建单 → … 的具体步骤/脚本>
- 有无种子数据脚本：<有，命令 / 无>
```

## 6. 数据库
- 结构文档：`DATA_MODEL.md`（表/字段/约束/注释，与库一致）
- 迁移方式：<工具 + 命令>；迁移目录：<路径>；**旧迁移不可改，只加新的**
- 连接/查看：<只读连接方式 / GUI 工具建议>

## 7. 接口
- 接口文档：`API_SPEC.md`（或 OpenAPI：<路径/URL>）
- 鉴权方式：<JWT/Session>；如何拿测试 token：<步骤>

## 8. 关键业务规则与决策（避免踩雷）
- 重要规则：<如"券核销必须同事务防并发"，指向 cards/ 或代码位置>
- 关键技术决策与原因：见 `ARCHITECTURE.md` 决策表
- 开发历程与为什么这么做：见 `DEVLOG.md`

## 9. 测试与质量门禁
```
- 跑全部测试：<命令>（预计耗时 <x>，应全绿）
- 覆盖率：<阈值 / 如何看>
- pre-commit：测试不绿无法提交（.fishpond/verify.sh）
- CI：<在哪看，PR 必须绿才合并>
```

## 10. 已知问题与待办（诚实，不藏）
| # | 问题/待办 | 影响 | 建议处理 | 关联 |
|---|---|---|---|---|
| 1 | <如：订单超时未处理> | 中 | <方案> | F-0xx |

## 11. 负责人 / 联系人
- 业务对接：<>；技术负责：<>；运维：<>

---

## 交接确认清单（接手人逐条打勾，全绿才算交接成功）
```
☐ 按第 2 节能在本地把后端+前端跑起来
☐ 能连上数据库、迁移已生效
☐ 能跑通全部测试且全绿
☐ 能跑通第 5 节的核心链路冒烟
☐ 看 ARCHITECTURE + FEATURE_LIST 能说清系统在做什么、怎么分模块
☐ 随便挑一个功能，能顺着"功能清单→模块→代码"找到实现
☐ 已知问题清楚，知道下一步做什么
```
